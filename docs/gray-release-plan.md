# 灰度发布状态

`GrayReleaseManager` 保留了设备分桶组件，但当前稳定数据路径尚未连接这套 WebRTC 灰度决策。本仓库没有完成历史文档所示的运行时灰度管理 API、监控看板和自动回滚系统。

因此不要按历史“10% → 50% → 100%”示例认定功能已启用。当前升级应先备份原数据和配置，在测试环境执行 TCP/固定端口回归，再按实际部署流程逐台更新。操作步骤见 [用户手册](USER_MANUAL.md)。
