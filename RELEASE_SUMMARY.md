# 1.2.1 验证说明

本次修复针对长连接失效检测、重连资源清理和后台固定端口一致性。修改保留在开发分支，没有部署到公网。

## 已实际执行

最终验证日期：2026-09-13。

| 检查 | 结果 |
| --- | --- |
| Java 全量测试 | 141项，失败0，错误0，跳过3 |
| Go 客户端、协议、信令与Sidecar测试 | 通过 |
| Fyne真实GUI代码的软件驱动测试 | 通过 |
| Go客户端、协议和GUI的 race 检测 | 使用本机MinGW启用CGO，全部通过 |
| PowerShell / Bash构建脚本契约 | 通过，含拒绝覆盖与明确GUI失败的场景 |
| Windows JAR、CLI、原生GUI、Sidecar | 构建成功，位于release/outview-1.2.1 |
| 独立代码审查 | 三项Important均已修复并针对性复核 |

安装器脚本已整理并用替身编译器验证参数传递，当前未使用真实Inno Setup生成安装包；可直接使用GUI可执行文件。

- 原代码上复现首次连接失败不重试、静默连接不恢复、控制写阻塞、本地服务拖住控制消息、远端关闭不清理本地连接。
- Java 新增真实 H2/业务组件与生产 Netty 处理器测试，覆盖端口预留、冲突、在线变更、心跳、旧会话、关闭传播、归属和慢接收者。
- Go 客户端、协议及真实 GUI 软件驱动测试通过；注册、超时、背压和停止竞态有行为回归。
- 真实 Java JAR + Go CLI：同一转发连接空闲125秒后64KiB数据一致；四连接并发共5MiB数据一致。
- 真实端口占用时保留旧映射，在线修改后会话/设备查询/新监听一致。
- 修改端口后立即结束 JVM，再启动时保留固定端口并由 CLI 自动重连；封禁保留预留。
- 浏览器连接实际后台，验证登录、离线固定端口预设、修改和删除；另验证动态文本和范围校验。

完整可执行验收入口见 [开发指南](docs/DEVELOPER_GUIDE.md)，原始运行日志由测试命令输出到本地，不作为发布包内容。

## 边界

125秒回环结果不外推为公网、真实 Windows RDP 或小时级稳定性保证。没有用户现场断开日志，不能断言所有外部中断都来自同一个代码缺陷。网络真实断开时，原TCP流会结束，恢复后需要重新建立连接。

WebRTC 仍是实验模块。历史 NAT/弱网模拟结果和内存微基准不作为当前生产性能指标。GUI原生编译成功不等于已完成真实桌面窗口、系统托盘和RDP登录验收。
