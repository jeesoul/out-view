# outView 用户手册

当前操作说明统一维护在 [docs/USER_MANUAL.md](docs/USER_MANUAL.md)，包括部署、后台固定端口、客户端配置及保留原数据的升级步骤。

运行发布包时，从包根目录执行 `scripts/start-server.ps1` 或 `scripts/start-server.sh`。发布包应携带 `docs/`；如果你只复制了单个 JAR，请从源码仓库阅读完整手册。
