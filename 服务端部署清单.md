# 服务端部署清单

## 必须完成的步骤

### 1. 服务器准备
- [ ] 确认有 120.27.214.55 的 SSH 访问权限
- [ ] 确认服务器已安装 JDK 8+
- [ ] 确认防火墙开放端口：
  - 7000 (控制端口)
  - 6000-6500 (数据端口)
  - 8080 (管理后台，可选)

### 2. 部署服务端
```bash
# 方式一：使用部署脚本（推荐）
bash deploy-server.sh

# 方式二：手动部署
scp release/outview-1.2.0/outview-server.jar root@120.27.214.55:/opt/outview/
ssh root@120.27.214.55
cd /opt/outview
nohup java -jar outview-server.jar > server.log 2>&1 &
```

### 3. 验证部署
```bash
# 检查服务是否运行
ssh root@120.27.214.55 'netstat -tlnp | grep 7000'

# 查看日志
ssh root@120.27.214.55 'tail -f /opt/outview/server.log'

# 测试连接
telnet 120.27.214.55 7000
```

### 4. 防火墙配置
```bash
# CentOS/RHEL
firewall-cmd --permanent --add-port=7000/tcp
firewall-cmd --permanent --add-port=6000-6500/tcp
firewall-cmd --reload

# Ubuntu/Debian
ufw allow 7000/tcp
ufw allow 6000:6500/tcp
ufw reload

# 阿里云/腾讯云
# 在控制台安全组添加规则：
# - 入站：TCP 7000
# - 入站：TCP 6000-6500
```

### 5. 测试端到端连接
- [ ] 启动客户端（被控端）
- [ ] 查看设备码
- [ ] 启动客户端（控制端）
- [ ] 输入设备码连接
- [ ] 验证远程桌面是否正常

---

## 当前状态

### ✅ 已完成
- 客户端代码（硬编码 120.27.214.55:7000）
- 服务端 JAR 包（30MB）
- 发布包构建
- 文档和使用指南

### ❌ 未完成（必须）
- **部署服务端到 120.27.214.55**
- 开放防火墙端口
- 端到端测试

### ❌ 未完成（可选）
- Windows 安装包编译
- 开机自启配置
- 监控和日志

---

## 快速部署命令

如果你有 120.27.214.55 的访问权限：

```bash
# 1. 修改 deploy-server.sh 中的 USER 变量
# 2. 运行部署脚本
bash deploy-server.sh

# 3. 检查服务状态
ssh root@120.27.214.55 'systemctl status outview'

# 4. 查看日志
ssh root@120.27.214.55 'journalctl -u outview -f'
```

---

## 没有服务器怎么办？

### 选项 1：修改代码使用本地测试
1. 修改 `client/internal/devicecode/devicecode.go`
2. 改为 `ServerHost = "localhost"` 或你的测试服务器
3. 重新编译客户端
4. 本地运行服务端测试

### 选项 2：购买云服务器
- 阿里云/腾讯云轻量应用服务器（最低配即可）
- 1核2G，约 60 元/月
- 获得公网 IP 后替换代码中的地址

### 选项 3：使用现有服务器
如果你有其他公网服务器，修改代码中的地址即可

---

## 重要提醒

⚠️ **没有部署服务端，客户端无法使用！**

客户端启动后会尝试连接 120.27.214.55:7000，如果服务端未运行：
- 被控端：显示"连接服务器失败"
- 控制端：无法查询设备码

**必须先部署服务端，产品才能正常工作。**
