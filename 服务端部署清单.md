# 服务端部署清单

- 准备JDK8及需要开放的HTTP、控制和数据端口。
- 配置自己的管理账号，核对当前认证和TLS边界。
- 明确持久数据目录，使用 OUTVIEW_DATA_DIR 绝对路径，升级时保留原数据库和配置。
- 使用 scripts/start-server.ps1 或 scripts/start-server.sh 固定工作目录启动。
- 核对控制端口启动日志、后台固定预留数量及一次真实数据往返。
- 完成断线重连、端口冲突和服务重启后的固定端口验证。

实际配置与迁移步骤见 [用户手册](docs/USER_MANUAL.md)。仓库脚本不会自动部署到历史公网服务器。
